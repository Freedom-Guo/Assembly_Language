.386
STACK SEGMENT USE16 STACK
    DB 200 DUP(0)
STACK ENDS

DATA SEGMENT USE16
XUEHAO DB 4 DUP(0)
DATA ENDS

CODE SEGMENT USE16
        ASSUME CS:CODE,DS:DATA,SS:STACK
START: MOV AX,DATA
       MOV DS,AX
       MOV BX,OFFSET XUEHAO
       MOV BYTE PTR [BX], '4'
       MOV XUEHAO+1, '8'
       MOV BYTE PTR [BX+2], '4'
       MOV SI,3
       MOV BYTE PTR [BX+SI],'8'
       MOV AH,4CH;exit
       INT 21H
CODE ENDS
     END START

